docker.runOptions = '-u $(id -u):$(id -g)'
docker.enabled = true

params {

    readFoler = "$projectDir/data/fastq"
    result = "Results"
    reference = "$projectDir/data/Reference/H293.dna"
    local = true
}


